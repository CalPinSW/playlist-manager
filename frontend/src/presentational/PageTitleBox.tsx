import React, { FC } from "react";

interface IPageTitleBox {
  title: string;
  description: string;
}

const PageTitleBox: FC<IPageTitleBox> = ({title, description}) => {
    return (
        <div>
            
        </div>
    )
};
