[tool.poetry]
name = "playlist-manager"
version = "0.1.0"
description = ""
authors = ["CalPin <calum.pinder@softwire.com>"]
license = "MIT"
readme = "README.md"
packages = [
    { include = "src" }
    ]

[tool.poetry.dependencies]
python = "^3.10"
flask = "^3.0.0"
python-dotenv = "^1.0.0"
flask-cors = "^4.0.0"
pydantic = "^2.6.4"
requests = "^2.31.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
