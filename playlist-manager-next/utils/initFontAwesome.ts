// FontAwesome library registration for client-side use.
// This is a no-op stub so the test setup file can import it safely.
// In production, FontAwesome icons are imported directly via @fortawesome/react-fontawesome.
const initFontAwesome = () => {};
export default initFontAwesome;
